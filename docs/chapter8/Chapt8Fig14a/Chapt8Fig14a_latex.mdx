---
title: Chapter 8 - Fig 14A - LaTeX
sidebar_label: Fig14a-latex
---

# Chapter 8 - Fig 14A - LaTeX Document

<div style={{display: 'flex', gap: '12px', flexWrap: 'wrap', marginBottom: '24px'}}>
  <a href="/programs/latex/Chapt8Fig14a/Chapt8Fig14a.tex" download="Chapt8Fig14a.tex"
    style={{padding: '10px 20px', backgroundColor: '#10b981', color: 'white', borderRadius: '6px', textDecoration: 'none', fontWeight: 'bold'}}>
    📥 Download .tex
  </a>
  <a href="/programs/latex/Chapt8Fig14a/Chapt8Fig14a.tex" target="_blank" rel="noopener noreferrer"
    style={{padding: '10px 20px', backgroundColor: '#3b82f6', color: 'white', borderRadius: '6px', textDecoration: 'none', fontWeight: 'bold'}}>
    🔗 Open Raw
  </a>
</div>

## LaTeX Source

```latex title="Chapt8Fig14a.tex"
\documentclass[11pt,a4paper]{article}
\usepackage[utf8]{inputenc}
\usepackage{amsmath,amssymb,amsthm}
\usepackage{physics}
\usepackage{listings}
\usepackage{xcolor}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{booktabs}

\geometry{margin=1in}

\lstset{
    language=Matlab,
    basicstyle=\ttfamily\small,
    keywordstyle=\color{blue},
    commentstyle=\color{green!60!black},
    stringstyle=\color{orange},
    numbers=left,
    numberstyle=\tiny\color{gray},
    breaklines=true,
    frame=single,
    backgroundcolor=\color{gray!10}
}

\newtheorem{axiom}{Axiom}
\newtheorem{definition}{Definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{corollary}{Corollary}

\title{\textbf{Chapt8Fig14a.m} \\ Total Elastic Ionized Impurity Scattering Rate \\ \large Thomas-Fermi Screening with Angular Integration}
\author{Semiconductor Physics Documentation}
\date{}

\begin{document}

\maketitle

\begin{abstract}
This document provides complete theoretical background and code analysis for \texttt{Chapt8Fig14a.m}, which calculates the total elastic scattering rate due to ionized impurities as a function of electron energy. The code employs Thomas-Fermi screening and numerical angular integration using the trapezoidal rule.
\end{abstract}

\tableofcontents
\newpage

%==============================================================================
\section{Theoretical Foundation}
%==============================================================================

\subsection{Ionized Impurity Scattering Theory}

\begin{axiom}[Fermi's Golden Rule for Scattering]
The scattering rate from state $\mathbf{k}$ to state $\mathbf{k}'$ is:
\begin{equation}
W_{\mathbf{k}\to\mathbf{k}'} = \frac{2\pi}{\hbar}|V_{\mathbf{k}\mathbf{k}'}|^2 \delta(E_{\mathbf{k}'} - E_{\mathbf{k}})
\end{equation}
\end{axiom}

\begin{definition}[Screened Coulomb Matrix Element]
For ionized impurity scattering with Thomas-Fermi screening:
\begin{equation}
V_{\mathbf{k}\mathbf{k}'} = \frac{e^2}{\varepsilon_0\varepsilon_r} \cdot \frac{1}{q^2 + q_{\text{TF}}^2}
\end{equation}
where $q = |\mathbf{k}' - \mathbf{k}|$ is the momentum transfer.
\end{definition}

\subsection{Total Scattering Rate}

\begin{theorem}[Total Elastic Scattering Rate]
The total scattering rate is obtained by integrating over all final states:
\begin{equation}
\frac{1}{\tau(E)} = \sum_{\mathbf{k}'} W_{\mathbf{k}\to\mathbf{k}'} = n_i \int |V(q)|^2 g(\mathbf{k}') \delta(E' - E) \, d^3k'
\end{equation}
where $n_i$ is the impurity concentration.
\end{theorem}

\begin{theorem}[Angular Integration Form]
For elastic scattering on a spherical Fermi surface:
\begin{equation}
\frac{1}{\tau(E)} = \frac{\pi n_i m^*}{\hbar^3 k^3} \left(\frac{e^2}{4\pi\varepsilon_0\varepsilon_r}\right)^2 \int_0^\pi \frac{\sin\theta}{(q^2 + q_{\text{TF}}^2)^2} \, d\theta
\end{equation}
where $q = 2k\sin(\theta/2)$.
\end{theorem}

\subsection{Change of Variables}

\begin{lemma}[Substitution to $\eta$]
With $\eta = \sin(\theta/2)$:
\begin{itemize}
\item $q = 2k\eta$
\item $d\eta = \frac{1}{2}\cos(\theta/2)\,d\theta$
\item $\sin\theta = 2\sin(\theta/2)\cos(\theta/2) = 2\eta\sqrt{1-\eta^2}$
\end{itemize}
\end{lemma}

The integral becomes:
\begin{equation}
\int_0^\pi \frac{\sin\theta}{(q^2 + q_{\text{TF}}^2)^2} \, d\theta = \int_0^1 \frac{4\eta\sqrt{1-\eta^2}}{(4k^2\eta^2 + q_{\text{TF}}^2)^2} \cdot \frac{2d\eta}{\sqrt{1-\eta^2}}
\end{equation}

\subsection{Thomas-Fermi Screening}

\begin{definition}[Thomas-Fermi Wavevector]
\begin{equation}
q_{\text{TF}}^2 = \frac{k_F m^* e^2}{\pi^2 \hbar^2 \varepsilon_0 \varepsilon_r}
\end{equation}
where $k_F = (3\pi^2 n)^{1/3}$ is the Fermi wavevector.
\end{definition}

%==============================================================================
\section{Numerical Method: Trapezoidal Integration}
%==============================================================================

\subsection{The Trapezoidal Rule}

\begin{theorem}[Trapezoidal Rule]
For a function $f(x)$ on interval $[a, b]$ with $N$ subintervals of width $h = (b-a)/N$:
\begin{equation}
\int_a^b f(x)\,dx \approx h\left[\frac{f(a) + f(b)}{2} + \sum_{i=1}^{N-1} f(a + ih)\right]
\end{equation}
\end{theorem}

\begin{theorem}[Error Estimate]
The error in the trapezoidal rule is:
\begin{equation}
E_T = -\frac{(b-a)^3}{12N^2} f''(\xi)
\end{equation}
for some $\xi \in [a, b]$. The method is second-order accurate: $E_T = O(h^2)$.
\end{theorem}

\subsection{Application to Angular Integration}

The code uses a simplified form where the step size equals the increment:
\begin{equation}
\int_0^\pi f(\theta)\,d\theta \approx \sum_{j=1}^{180} f(\theta_j) \cdot \Delta\theta
\end{equation}
where $\Delta\theta = \pi/180$ radians (1° steps).

%==============================================================================
\section{Line-by-Line Code Analysis}
%==============================================================================

\subsection{Function Declaration}

\begin{lstlisting}
function TFelasticScattering
clear;clf;
\end{lstlisting}
\textit{Declares as a function and clears workspace.}

\subsection{Physical Constants}

\begin{lstlisting}
  m0=9.109382e-31;
echarge=1.6021764e-19;
hbar=1.05457159e-34;
epsilon0=8.8541878e-12;
\end{lstlisting}
\begin{equation}
m_0, e, \hbar, \varepsilon_0
\end{equation}
\textit{Fundamental physical constants in SI units.}

\vspace{0.5em}
\begin{lstlisting}
ezero=13.2;
m1=0.070;
\end{lstlisting}
\begin{equation}
\varepsilon_r = 13.2, \quad m^* = 0.07 m_0
\end{equation}
\textit{GaAs material parameters.}

\vspace{0.5em}
\begin{lstlisting}
emax=300.0d-3;
n=1.0d16;
npoints = 200;
\end{lstlisting}
\begin{equation}
E_{\max} = 300 \text{ meV}, \quad n_{\text{initial}} = 10^{16} \text{ cm}^{-3}
\end{equation}
\textit{Maximum energy for plot and starting carrier density.}

\subsection{Carrier Density Loop}

\begin{lstlisting}
  for k=1:2
   n=n*10;
\end{lstlisting}
\begin{equation}
n \in \{10^{17}, 10^{18}\} \text{ cm}^{-3}
\end{equation}
\textit{Loop over two carrier densities (plotted as two curves).}

\vspace{0.5em}
\begin{lstlisting}
 kF1=(3*(pi^2)*n)^(1/3);
 kF1=kF1*1.e2;
\end{lstlisting}
\begin{equation}
k_F = (3\pi^2 n)^{1/3} \times 10^2 \text{ (convert cm}^{-1} \to \text{m}^{-1}\text{)}
\end{equation}
\textit{Fermi wavevector calculation and unit conversion.}

\vspace{0.5em}
\begin{lstlisting}
    kF2=kF1^2;
    kF3=kF1^3;
\end{lstlisting}
\begin{equation}
k_F^2, k_F^3
\end{equation}
\textit{Powers of Fermi wavevector for later use.}

\subsection{Thomas-Fermi Screening Wavevector}

\begin{lstlisting}
 qTF2=kF1*m0*m1*(echarge^2)/(epsilon0*ezero*(pi^2)*(hbar^2));
\end{lstlisting}
\begin{equation}
q_{\text{TF}}^2 = \frac{k_F m_0 m_1 e^2}{\varepsilon_0 \varepsilon_r \pi^2 \hbar^2}
\end{equation}
\textit{Thomas-Fermi wavevector squared.}

\subsection{Fermi Energy}

\begin{lstlisting}
 eF=((hbar*kF1)^2)/(2.0*m0*m1*echarge);
\end{lstlisting}
\begin{equation}
E_F = \frac{(\hbar k_F)^2}{2m^*} \cdot \frac{1}{e} \text{ [eV]}
\end{equation}
\textit{Fermi energy in eV (division by $e$ converts from J to eV).}

\subsection{Constant Prefactor}

\begin{lstlisting}
 const=(pi*n*1.0e6*(echarge^4)*m0*m1);
 const=const/(((4.*pi*epsilon0)^2)*(ezero^2)*(hbar^3));
\end{lstlisting}
\begin{equation}
C = \frac{\pi n \cdot 10^6 \cdot e^4 \cdot m_0 m_1}{(4\pi\varepsilon_0)^2 \varepsilon_r^2 \hbar^3}
\end{equation}
\textit{Prefactor for scattering rate (includes unit conversion).}

\subsection{Energy Grid Setup}

\begin{lstlisting}
 emin=eF;
 demax=(emax-emin)/200.0;
 dth=pi/180.0;
\end{lstlisting}
\begin{equation}
E_{\min} = E_F, \quad \Delta E = \frac{E_{\max} - E_F}{200}, \quad \Delta\theta = \frac{\pi}{180}
\end{equation}
\textit{Energy step and angular step (1° increments).}

\subsection{Main Energy Loop}

\begin{lstlisting}
 ei=emin;
 for i=1:npoints
   ei=ei+demax;
   ak=(sqrt(2.*m0*m1*ei*echarge))/hbar;
   ak3=ak^3;
\end{lstlisting}
\begin{equation}
k = \frac{\sqrt{2m^* E \cdot e}}{\hbar}, \quad k^3
\end{equation}
\textit{Electron wavevector at each energy.}

\subsection{Angular Integration Loop}

\begin{lstlisting}
   aint1=0.0;
   theta=0.0;
  for j=1:180
     theta=theta+dth;
     aint1=aint1+(dth*gchi1(theta,ak,qTF2));
  end
\end{lstlisting}
\begin{equation}
I = \sum_{j=1}^{180} g(\theta_j) \cdot \Delta\theta
\end{equation}
\textit{Trapezoidal integration over angle (calls helper function gchi1).}

\subsection{Store Results}

\begin{lstlisting}
	y1(i)=const*aint1/ak3;
   x1(i)=ei*1.d3;
\end{lstlisting}
\begin{equation}
\frac{1}{\tau(E)} = \frac{C \cdot I}{k^3}, \quad E \text{ [meV]}
\end{equation}
\textit{Scattering rate calculation and energy in meV.}

\subsection{Helper Function: gchi1}

\begin{lstlisting}
function gchi1 = gchi1(theta,k,qTF2) 
 x=2.0*k*sin(theta/2.0); 
 x2=x^2;	 
 g=(tan(theta/2.0))*((sin(theta/2.))^2)*((1.0+(qTF2/x2))^2); 
 gchi1=1.0/g;
return
\end{lstlisting}
\begin{equation}
g(\theta) = \frac{1}{\tan(\theta/2) \cdot \sin^2(\theta/2) \cdot (1 + q_{\text{TF}}^2/q^2)^2}
\end{equation}
where $q = 2k\sin(\theta/2)$.

\textit{This computes the integrand for the angular integration.}

\subsection{Plotting}

\begin{lstlisting}
figure(1);hold on;plot(x1,y1,'r');axis([0,emax*1.d3,0,16e12]);
\end{lstlisting}
\textit{Plot scattering rate vs energy with axis limits.}

%==============================================================================
\section{Physical Interpretation}
%==============================================================================

\subsection{Energy Dependence}

The scattering rate $1/\tau(E)$ typically:
\begin{itemize}
\item Decreases with increasing energy (faster electrons harder to scatter)
\item Scales approximately as $E^{-3/2}$ at high energy
\item Has a plateau or peak near $E_F$
\end{itemize}

\subsection{Density Dependence}

Higher carrier density leads to:
\begin{itemize}
\item Stronger screening (larger $q_{\text{TF}}$)
\item Lower scattering rate at small angles
\item But higher overall scattering due to more impurities (if $n_i = n$)
\end{itemize}

%==============================================================================
\section{Summary}
%==============================================================================

This code calculates the total elastic scattering rate by numerically integrating over all scattering angles using the trapezoidal rule. The helper function \texttt{gchi1} encodes the screened Coulomb scattering matrix element with Thomas-Fermi screening. Results show how scattering rate varies with electron energy and carrier density.

\end{document}

```

---

[← Back to Chapter 8 - Fig 14A](./)
