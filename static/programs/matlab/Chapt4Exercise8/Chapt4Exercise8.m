%Chapt4Exercise8.m%eigenstates of resonant tunnel barrier%clearclf;length = 5;											%length of well (nm)N=1000;												%number of sample pointsx=0:length/N:length;								%position of sample points in potentialmass=1.00;											%effective electron mass / m0num_sol=2;											%number of solutions soughtv0=1;													%potential scale (eV) v=v0.*ones(1,N+1); % first well for i=341:460										%well width 120*length/N   = 0.6 nm    v(i)=0; end														%barrier width 81*length/N = 0.4 nm% second well for i=540:659										%well width 120*length/N   = 0.6 nm    v(i)=0; end[energy,phi]=solve_schM(length,N,v,mass,num_sol);                          %call function solve_schM      for i=1:num_solsprintf(['eigenfunction (',num2str(i),') = ',num2str(energy(i)),' eV'])		%energy eigenvalues   end     figure(1); plot(x,v,'b');xlabel('Distance (nm)'),ylabel('Potential energy, (eV)'); ttl=['Chapt4Exercise8, m_{eff} = ',num2str(mass),' x m_0, Length = ',num2str(length),' nm']; title(ttl);s=char('y','k','r','g','b','m','c');		%plot curves in different colorsfigure(2);for i=1:num_sol   j=1+mod(i,7);									%select color for plot   	plot(x,phi(:,i),s(j));					%plot eigenfunctions     hold on;  end xlabel('Distance (nm)'),ylabel('Wave function'); title(ttl);hold off;